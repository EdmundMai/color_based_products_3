<%= link_to "New Coupon", new_admin_coupon_path %>


<%#= will_paginate site_wide_coupons, params: { q: @search_terms }, class: 'admin_pagination' %>

<table>
  <thead>
    <th>ID</th>
    <th>Name</th>
    <th>Amount</th>
    <th>Code</th>
  </thead>


  <% @coupons.each do |coupon| %>
  <tr>
    <td><%= link_to coupon.id, edit_admin_coupon_path(coupon) %></td>
    <td><%= coupon.name %></td>
    <td><%= coupon.value_prettified %></td>
    <td><%= coupon.code %></td>
  </tr>
  <% end %>
</table>